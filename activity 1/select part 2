SELECT CONCAT(fname, ' ', lname) as fullname, membership.status, reports.total_amount as amount, reports.total_amount - (reports.total_amount * 10/100) AS discounted FROM client_member INNER JOIN membership ON client_member.client_ID = membership.client_ID INNER JOIN reports ON client_member.client_ID = reports.client_ID;